# Worker independiente (NO Cloudflare Pages) para disparar los recordatorios
# horarios y la escalacion/liberacion de horario de citas "Preaprobadas" del
# chatbot que no se confirman a tiempo. Ver workers/booking-reminder-cron/README.md
# para el paso a paso de configuracion y despliegue (ninguno de esos pasos se
# ejecuto como parte de esta tarea).
name = "dalfi-erp-booking-reminder-cron"
main = "worker.js"
compatibility_date = "2026-07-09"

# Variable NO secreta: el backend real (Render + Neon) al que este Worker le hace fetch().
# dalfi-erp.pages.dev (Cloudflare Pages + Supabase) se eliminó -- la lógica de recordatorios se
# portó a POST /api/booking/send-reminders en server/app.mjs (ver ahí checkConfirmationReminder).
# Este Worker solo tiene manejador `scheduled`: no atiende peticiones HTTP de nadie. Sin estas
# dos lineas, wrangler le habilita por defecto una URL publica (*.workers.dev) y URLs de vista
# previa que no sirven para nada y son superficie de mas.
workers_dev = false
preview_urls = false

[vars]
APP_BASE_URL = "https://ssc.dalfistudio.com"
# El bot, para el barrido de "sigo esperando a una persona" (ver runHumanWaitSweep en worker.js).
# Su secreto va aparte: npx wrangler secret put ERP_WEBHOOK_SECRET
BOT_BASE_URL = "https://bot.dalfistudio.com"

# BOOKING_REMINDER_CRON_SECRET se configura por separado como Secret (nunca
# en este archivo ni en Git): "wrangler secret put BOOKING_REMINDER_CRON_SECRET".
# Debe ser EXACTAMENTE el mismo valor que env.BOOKING_REMINDER_CRON_SECRET en el servicio de
# Render (dalfi-erp) que ya usa POST /api/booking/send-reminders en server/app.mjs.

# Una vez por hora. Cloudflare Cron Triggers usan UTC; da igual la hora exacta
# porque checkPreapprovedConfirmationReminder() en outputs/lib/booking-engine.js
# decide internamente, con cada disparo, si hace falta enviar recordatorio o
# escalar segun cuanto falte para cada cita — este Worker solo necesita
# ejecutarse con frecuencia suficiente (cada hora) para que esa ventana nunca
# se salte un ciclo.
[triggers]
crons = ["*/5 * * * *"]
